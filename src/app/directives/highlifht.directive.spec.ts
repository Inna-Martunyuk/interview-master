import { HighlifhtDirective } from './highlifht.directive';

describe('HighlifhtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlifhtDirective();
    expect(directive).toBeTruthy();
  });
});
